spring:
  application:
    name: web-app-jdbc
  datasource:
    url: jdbc:avatica:remote:url=http://localhost:18081; use_client_side_lb=true; lb_urls=http://localhost:18080,http://localhost:18081
    driver-class-name: org.apache.calcite.avatica.remote.Driver
server:
  port: 9080
